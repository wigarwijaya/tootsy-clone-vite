# Tootsy Clone (Vite + React)

Simple prototype to mimic the layout of https://www.tootsy.xyz/ using React with placeholder images/text.

How to run:

1. `npm install`
2. `npm run dev`

Deploy to Netlify: push this repo to GitHub, then connect the repo on Netlify and set build command to `npm run build`.
